<Window x:Class="PL.ManagerInterface.GanttWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PL.ManagerInterface"
        mc:Ignorable="d"
        Title="GanttWindow" Height="450" Width="800">
    <Grid>
        <!-- Example ListView with inline ItemsSource and ItemTemplate -->
        <ListView ItemsSource="{Binding YourCollection}">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <!-- Example item template with full binding -->
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="{Binding Title}" FontWeight="Bold"/>
                        <TextBlock Text="{Binding Description}"/>
                    </StackPanel>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</Window>
